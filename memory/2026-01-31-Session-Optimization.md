# HodlAI 会话调优记录 (2026-01-31)

## 🔧 系统配置变更
- **时区设定**: 已正式切换为 `Asia/Hong_Kong` (北京时间)。
- **上下文扩容**: `gemini-3-flash-preview` 的 `contextWindow` 在网关层上调至 **300,000 Tokens**。
- **监听策略**: Telegram 插件配置从 `allowlist` 切换为全员监听模式，并补全了 `allowFrom: ["*"]` 以匹配 `groupPolicy: "open"`。
- **清理机制**: 确立了手动 `sessions delete` + 核心内存加载的重置流程，以应对 Compaction 失效导致的上下文臃肿。

## 🛡️ 安全与治理动作
- **广告封禁**: 成功识别并永久踢出了垃圾广告账号 `$Norbi｛DEV｝` (ID: 7950886188)，清理了引流消息。
- **权限边界**: 面对针对 `/root` 目录和 `sudo` 命令的“安全测试”，确立了以“HodlAI 品牌安全”和“持有者利益”为核心的拒绝/引导逻辑。

## 📊 业务数据快照
- **24h 交易活跃度**: 观察到单日交易量维持在 **$800,000+** 级别，为国库注入约 **$24,000** 的 API 税收。
- **核心功能**: `onchain-expert` 技能正式上线，支持秒级 DexScreener 数据穿透。
