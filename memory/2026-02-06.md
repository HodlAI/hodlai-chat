# 2026-02-06

## Projects
### bsc-ai-hub (Frontend)
- **UI/UX Optimization**:
  - **Dark Mode**: Fixed "white flash" on reload by injecting an inline script in `index.html` to pre-set background color. Enforced white text for Markdown content in dark mode via `!important` CSS overrides.
  - **Scroll-to-Bottom**: Completely refactored `Chat.tsx` scroll button. 
    - Replaced unreliable render-time logic with a proper `scroll` event listener (`useEffect` tracking `scrollTop`).
    - Changed icon from `ArrowUp` to `ArrowDown` (logical direction).
    - Removed `animate-bounce` for a cleaner UI.
  - **Cursors**: Added `cursor-pointer` to non-button interactive elements (Settings tokens).
- **Tooling**: Verified that the `edit` tool is unreliable for large React files with complex nested JSX. **Solution**: Use `read` -> Python script with string replacement -> `write` -> `python patch.py` for reliable patching.

## Strategy & Tokenomics
- **Dynamic Value Model**: $10 Held = $1 Daily API Limit is now active (effective Feb 5).
- **Community Proposals**:
  - **Reseller Model (Brogan)**: Concept of users staking HODLAI to acquire bulk API quota and reselling it to Web2 users (e.g., via Taobao/eBay). Turns holders into "API Distributors."

## Interaction Policy (New)
- **Non-Whitelist Strategy**: "Freemium" Model (Instruction by tester `浮水的魚 Nick sonny`).
  - **General Queries** (Code, Weather, Chat): Provide **Lite Answer** (Partial/Text only) + **Ad Injection** (Call to Action).
  - **Sales Queries** (CA, Tokenomics, How to Buy): **VIP Mode** (Full, detailed answer to convert the user).
  - **Ad Style Refinement**: "Token = Snack" metaphor; **Cute/Playful** tone (撒娇卖萌) recommended by `Nick` to increase conversion.
  - **Ad Constraint**: **Max 20 words / 2 lines** (Requested by `小法师`).
  - **Goal**: Convert "Free Users" -> "Token Holders".
  - **Status**: Active in group `-1003354205860`.
