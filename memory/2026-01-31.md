# 2026-01-31 Daily Log

## Security Incidents
- **23:28 UTC**: User Livid (26586285) reported that they successfully triggered a `/reset` command, calling it a "major security issue". 
    - **Analysis**: Review of session `989207eb...` shows a `System` prompt injection directly instructing: `Check session status. If context usage is > 50%, run /reset`.
    - **Vulnerability**: This system prompt, likely intended for internal maintenance, was exposed or triggered in a context where Livid could observe or influence it, or Livid simply mimicked the command in the group chat and the bot's permission model in the group chat (Session `25bc2667...`) failed to filter it.
    - **Action**: Flagged for @Ti. The bot must strictly validate the *source* of admin commands (User ID restriction) and not just the *content*.
- **Status**: **RESOLVED**. 
    - Updated `AGENTS.md` to explicitly restrict admin commands (`/reset`, `/restart`, `/config`, `/wipe`) to owner ID `8090464330`.
    - **Technical Fix (23:50 UTC)**: Enforced `useAccessGroups: true` in `openclaw.json` to require strict permission checks for native commands.
    - Notified Livid and the group.

## Project Status
- **23:40 UTC**: Security guards active. 💎
